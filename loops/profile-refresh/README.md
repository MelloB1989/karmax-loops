# profile-refresh

**Default loop.** Every 12 hours the agent re-reads recent memory and rewrites the
curated `ABOUT_ME.md` profile (via `profile.update`) so its model of you — identity,
projects, preferences, relationships, goals — reflects the latest truth. Facts that
are still valid are preserved. No configuration.
